#include<stdio.h>/*输入一个数，判断是否为素数*/
int main()
{
	int a,i;
	scanf("%d",&a);
	for(i=2;i<a;i++)
	{
    	if(a%i==0)
	    {
	       printf("不是素数");
	       break;
	    }
    }
    if(a==i)
	printf("是素数");
	
}
